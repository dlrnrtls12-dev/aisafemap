# AGENTS.md

이 문서는 GitHub Copilot, Codex 등 코딩 AI와 사람 개발자가 이 저장소에서 작업할 때 따라야 하는 개발 지침입니다.

## 1. 작업 전 확인

- 작업을 시작하기 전에 `docs/PRODUCT.md`와 작업에 관련된 `docs/ARCHITECTURE.md`, `docs/API.md`, `docs/DATABASE.md`, `docs/SECURITY.md`, `docs/DEPLOYMENT.md`를 읽습니다.
- `PRODUCT.md`에 없는 기능을 임의로 추가하지 않습니다.
- 요구사항이 문서와 충돌하면 코드를 먼저 바꾸지 말고 관련 문서를 수정하거나 사람의 승인을 받습니다.

## 2. 구현 단계 구분

`docs/PRODUCT.md`는 경진대회용 제품의 전체 범위를 설명합니다. 개발은 완성도를 높이기 위해 다음 단계로 나눕니다.

### MVP 1차 구현

- 사고방 생성
- 신고자용·소방대원용·지휘관용 접속 권한 분리
- 신고자 GPS 위치와 상태정보 입력
- 신고자와 구조대 위치 지도 표시
- 현재 기상정보와 일출·일몰 표시
- 산 정보와 산악안전시설 표시
- 규칙 기반 위험도 분석
- 구조장비 추천
- 실시간 문자 채팅
- 사고 종료 및 기록 저장

### MVP 2차 구현

- 실제 등산로 기반 최적 구조 경로
- 신고자용 응급처치 챗봇
- 지휘관용 SOP 챗봇

### MVP 이후

- STT
- TTS
- 영상통화
- 고급 수색구역 분석

한 번에 전체 기능을 만들지 말고, 현재 승인된 구현 단계의 작은 기능 단위로 작업합니다.

## 3. 기술 스택 고정

기술 스택을 임의로 변경하지 않습니다.

- 프론트엔드: React + TypeScript + Vite
- 지도: Leaflet
- 백엔드: Python FastAPI
- 데이터베이스: PostgreSQL + PostGIS
- 실시간 통신: FastAPI WebSocket
- 배포 및 개발환경: Docker Compose
- 테스트: Pytest와 프론트엔드 기본 테스트
- CI: GitHub Actions

새로운 프레임워크나 외부 서비스를 도입하려면 이유, 비용, 보안 영향, 대안을 문서화하고 승인을 받습니다.

## 4. 작은 단위로 작업

- 한 번에 몇 시간에서 하루 정도의 작은 기능 단위로 구현합니다.
- 큰 변경은 여러 커밋과 PR로 나눕니다.
- 변경한 동작에는 테스트를 추가하거나, 테스트 가능한 구조로 구현합니다.
- API가 바뀌면 `docs/API.md`도 함께 갱신합니다.
- 데이터 모델이 바뀌면 `docs/DATABASE.md`도 함께 갱신합니다.

## 5. 민감정보와 API 키

- API 키, 비밀번호, 접속 토큰, 개인정보를 코드·커밋 메시지·PR 본문·로그에 포함하지 않습니다.
- 이름, 전화번호, 건강정보, 전체 GPS 좌표를 로그에 남기지 않습니다.
- 비밀정보는 서버 환경변수 또는 GitHub Secrets로 관리합니다.
- 실제 API 연결 전에는 `DEMO_DATA_MODE=true`를 사용합니다.
- 시연 데이터는 실제 개인정보와 명확히 구분합니다.
- 설정 방법은 `docs/DEPLOYMENT.md`와 구현 후 생성될 `backend/app/core/config.py`를 기준으로 합니다.

## 6. AI 분석 결과

- 위험도와 장비 추천은 참고자료이며 현장지휘관과 구조대원의 판단을 대신하지 않습니다.
- UI와 API 응답에 다음 취지의 안내를 표시합니다.

> AI 분석 결과는 참고자료이며, 최종 판단은 현장지휘관과 구조대원이 합니다.

- AI가 출동 명령, 구조 중단, 환자 진단, 의료행위, 헬기 출동을 확정하지 않도록 합니다.

## 7. 작업 완료 보고

작업을 완료한 뒤 PR 본문이나 작업 보고에 다음 내용을 포함합니다.

1. 수정한 파일
2. 변경 이유
3. 실행 방법
4. 테스트 방법과 결과
5. 남은 문제 또는 TODO
6. 다음 권장 작업

## 8. 변경 제안 절차

외부 API, 데이터 보존 정책, 인증 방식, 기술 스택 또는 제품 범위를 바꾸려면 관련 문서를 먼저 갱신하고 사람의 승인을 받은 뒤 구현합니다.
